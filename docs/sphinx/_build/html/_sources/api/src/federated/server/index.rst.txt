src.federated.server
====================

.. py:module:: src.federated.server

.. autoapi-nested-parse::

   Federated learning server script for coordinating FedAvg training.

   Usage:
       python -m src.federated.server --model cnn --num-rounds 10 --clients-per-round 5



Attributes
----------

.. autoapisummary::

   src.federated.server.logger


Functions
---------

.. autoapisummary::

   src.federated.server.create_client_loaders
   src.federated.server.federated_train
   src.federated.server.main


Module Contents
---------------

.. py:data:: logger

.. py:function:: create_client_loaders(train_dataset: torch.utils.data.Dataset, num_clients: int, batch_size: int = 32, shuffle: bool = True, num_workers: int = 0, non_iid_alpha: Optional[float] = None) -> List[torch.utils.data.DataLoader]

   Partition training data across clients with optional non-IID distribution.

   Non-IID partitioning (when alpha < inf):
       Samples are distributed using Dirichlet distribution to create
       realistic non-IID data (some clients specialize in certain classes).

   :param train_dataset: PyTorch Dataset to partition
   :param num_clients: Number of clients
   :param batch_size: Batch size for each client loader
   :param shuffle: Whether to shuffle data within each client
   :param num_workers: Number of data loading workers
   :param non_iid_alpha: Dirichlet concentration parameter. Lower alpha → more non-IID.
                         If None, uses uniform (IID) partitioning.

   :returns: List of DataLoaders, one per client


.. py:function:: federated_train(model_name: str = 'cnn', num_rounds: int = 10, clients_per_round: int = 5, local_epochs: int = 1, learning_rate: float = 0.01, num_clients: int = 10, batch_size: int = 32, non_iid_alpha: Optional[float] = None, device: str = 'cpu', seed: int = 42, checkpoint_dir: str = 'checkpoints', log_file: Optional[str] = None) -> Tuple[torch.nn.Module, dict]

   Execute federated learning training.

   :param model_name: 'cnn', 'resnet', or 'efficientnet'
   :param num_rounds: Number of federation rounds
   :param clients_per_round: Number of clients sampled per round
   :param local_epochs: Local SGD epochs per client per round
   :param learning_rate: Global learning rate
   :param num_clients: Total number of clients
   :param batch_size: Batch size for each client
   :param non_iid_alpha: Dirichlet alpha for non-IID partitioning (None = IID)
   :param device: 'cuda' or 'cpu'
   :param seed: Random seed
   :param checkpoint_dir: Directory to save checkpoints
   :param log_file: Optional file to save metrics

   :returns: Tuple of (trained_model, results_dict)


.. py:function:: main() -> None

   CLI entry point for federated training.


