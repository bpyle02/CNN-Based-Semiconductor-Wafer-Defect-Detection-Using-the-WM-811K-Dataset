src.federated.aggregation
=========================

.. py:module:: src.federated.aggregation

.. autoapi-nested-parse::

   Byzantine-robust aggregation methods for federated learning.

   Implements robust aggregation strategies that can tolerate malicious or faulty
   clients in federated learning settings.



Classes
-------

.. autoapisummary::

   src.federated.aggregation.Krum
   src.federated.aggregation.MultiKrum
   src.federated.aggregation.ByzantineRobustAggregator


Module Contents
---------------

.. py:class:: Krum

   Krum aggregation: selects parameter vector closest to most others.

   Filters out potential Byzantine updates by selecting the gradient vector
   that is closest (in Euclidean distance) to the maximum number of other
   gradient vectors. Robust to f malicious clients where n >= 2f + 3
   (per Blanchard et al., 2017).


   .. py:method:: aggregate(client_updates: List[Dict[str, torch.Tensor]], byzantine_tolerance: int = 1) -> Dict[str, torch.Tensor]
      :staticmethod:


      Aggregate using Krum algorithm.

      :param client_updates: List of client parameter dicts
      :param byzantine_tolerance: Number of Byzantine clients to tolerate

      :returns: Aggregated parameter dict (single selected update)



.. py:class:: MultiKrum

   Multi-Krum: selects m parameter vectors via repeated Krum.

   Applies Krum m times, removing the selected vector each iteration.
   More robust than single Krum as it combines multiple diverse updates.


   .. py:method:: aggregate(client_updates: List[Dict[str, torch.Tensor]], num_selected: int = 2, byzantine_tolerance: int = 1) -> Dict[str, torch.Tensor]
      :staticmethod:


      Aggregate using Multi-Krum algorithm.

      :param client_updates: List of client parameter dicts
      :param num_selected: Number of vectors to select
      :param byzantine_tolerance: Number of Byzantine clients to tolerate

      :returns: Averaged aggregation of top-m selected vectors



.. py:class:: ByzantineRobustAggregator

   Byzantine-robust aggregation with multiple strategies.


   .. py:attribute:: STRATEGIES


   .. py:method:: aggregate(client_updates: List[Dict[str, torch.Tensor]], strategy: str = 'multi_krum', **kwargs: Any) -> Dict[str, torch.Tensor]
      :staticmethod:


      Aggregate client updates using specified strategy.

      :param client_updates: List of client parameter dicts
      :param strategy: Aggregation strategy ('krum', 'multi_krum', etc.)
      :param \*\*kwargs: Strategy-specific parameters

      :returns: Aggregated parameters

      :raises ValueError: If strategy not supported



