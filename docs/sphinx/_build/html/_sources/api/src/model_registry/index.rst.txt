src.model_registry
==================

.. py:module:: src.model_registry


Attributes
----------

.. autoapisummary::

   src.model_registry.logger
   src.model_registry.REPO_ROOT
   src.model_registry.DEFAULT_TRUSTED_CHECKPOINT_DIRS


Classes
-------

.. autoapisummary::

   src.model_registry.ModelMetadata
   src.model_registry.ModelRegistry


Functions
---------

.. autoapisummary::

   src.model_registry.resolve_checkpoint_path
   src.model_registry.resolve_trusted_checkpoint_path
   src.model_registry.compute_checkpoint_hash
   src.model_registry.save_checkpoint_with_hash
   src.model_registry.verify_checkpoint


Module Contents
---------------

.. py:data:: logger

.. py:data:: REPO_ROOT

.. py:data:: DEFAULT_TRUSTED_CHECKPOINT_DIRS

.. py:function:: resolve_checkpoint_path(path: Union[str, pathlib.Path]) -> pathlib.Path

   Resolve and validate a checkpoint path.


.. py:function:: resolve_trusted_checkpoint_path(path: Union[str, pathlib.Path], allowed_roots: Optional[Iterable[Union[str, pathlib.Path]]] = None) -> pathlib.Path

   Resolve a checkpoint path and ensure it is inside a trusted root.


.. py:function:: compute_checkpoint_hash(path: pathlib.Path) -> str

   Compute SHA-256 hash of a checkpoint file.

   :param path: Path to the checkpoint file.

   :returns: Hex-encoded SHA-256 digest.


.. py:function:: save_checkpoint_with_hash(state_dict: dict, path: pathlib.Path) -> str

   Save checkpoint and write a companion .sha256 hash file.

   :param state_dict: Data to pass to ``torch.save``.
   :param path: Destination file path.

   :returns: Hex-encoded SHA-256 digest of the saved file.


.. py:function:: verify_checkpoint(path: pathlib.Path) -> bool

   Verify checkpoint integrity against its stored SHA-256 hash.

   If no companion ``.sha256`` file exists the check is skipped for
   backwards compatibility and the function returns ``True``.

   :param path: Path to the checkpoint file.

   :returns: ``True`` if the file matches its hash or no hash file exists,
             ``False`` if the hash does not match.


.. py:class:: ModelMetadata(model_name: str, architecture: str, num_classes: int, training_config: Dict[str, Any], metrics: Dict[str, float], dataset_version: str)

   Metadata for saved models.


   .. py:attribute:: model_name


   .. py:attribute:: architecture


   .. py:attribute:: num_classes


   .. py:attribute:: training_config


   .. py:attribute:: metrics


   .. py:attribute:: dataset_version


   .. py:attribute:: timestamp


   .. py:attribute:: model_hash
      :value: ''



   .. py:attribute:: num_params
      :value: 0



.. py:class:: ModelRegistry(registry_path: str = 'model_registry')

   Centralized model storage and versioning.


   .. py:attribute:: registry_path


   .. py:attribute:: metadata_file


   .. py:attribute:: models


   .. py:method:: register(model: torch.nn.Module, metadata: ModelMetadata, version: str = 'v1.0') -> str

      Register and save model with metadata.



   .. py:method:: load(model_id: str) -> Tuple[Dict[str, torch.Tensor], Dict[str, Any]]

      Load model state dict and metadata by ID.



   .. py:method:: list_models() -> List[str]

      List all registered models.



   .. py:method:: compare(model_id1: str, model_id2: str) -> Dict[str, Any]

      Compare metrics between two models.



