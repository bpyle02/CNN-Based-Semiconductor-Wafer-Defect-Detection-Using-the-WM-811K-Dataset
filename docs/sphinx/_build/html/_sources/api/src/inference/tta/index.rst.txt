src.inference.tta
=================

.. py:module:: src.inference.tta

.. autoapi-nested-parse::

   Test-Time Augmentation (TTA) for wafer defect classification.

   Applies multiple geometric augmentations to each test image and averages
   the softmax predictions.  This reduces prediction variance, improves
   calibration, and typically boosts minority-class recall because the model
   sees each sample from several viewpoints.

   Default views (5):
       1. Identity (original image)
       2. Horizontal flip
       3. Vertical flip
       4. Horizontal + vertical flip (180-degree rotation equivalent)
       5. 90-degree rotation

   .. rubric:: References

   Simonyan & Zisserman (2015). "Very Deep ConvNets" (TTA at test time).
   Shanmugam et al. (2021). "Better Aggregation in TTA". arXiv:2011.11156



Attributes
----------

.. autoapisummary::

   src.inference.tta.logger


Classes
-------

.. autoapisummary::

   src.inference.tta.TestTimeAugmentation


Functions
---------

.. autoapisummary::

   src.inference.tta.predict_with_tta


Module Contents
---------------

.. py:data:: logger

.. py:function:: predict_with_tta(model: torch.nn.Module, x: torch.Tensor, augmentations: Optional[List] = None) -> torch.Tensor

   Return averaged softmax probabilities across TTA augmentations.

   Default augmentations (8 total):
     - identity
     - hflip
     - vflip
     - rot90, rot180, rot270
     - hflip + rot90
     - vflip + rot90

   :param model: PyTorch classifier returning logits of shape ``(B, num_classes)``.
   :param x: Input batch tensor with shape ``(B, C, H, W)``.
   :param augmentations: Optional list of callables ``(B,C,H,W) -> (B,C,H,W)``.
                         Defaults to the 8-view zero-copy rotation/flip set.

   :returns: Averaged softmax probabilities of shape ``(B, num_classes)``.

   .. rubric:: Notes

   Softmax outputs for classification are rotation/flip invariant in
   label space, so no inverse transform is needed before averaging.


.. py:class:: TestTimeAugmentation(model: torch.nn.Module, transforms_list: Optional[List[torchvision.transforms.Compose]] = None, device: str = 'cpu')

   Test-time augmentation: average predictions over augmented views.

   Applies multiple augmentations to each test image and averages the
   softmax predictions. Improves calibration and minority class recall.

   :param model: Trained PyTorch classifier.
   :param transforms_list: Optional list of ``transforms.Compose`` pipelines.
                           If ``None``, the five default geometric views are used.
   :param device: Compute device (``"cpu"`` or ``"cuda"``).


   .. py:attribute:: model


   .. py:attribute:: device
      :value: 'cpu'



   .. py:property:: num_views
      :type: int


      Number of augmented views used for prediction.


   .. py:method:: predict(images: torch.Tensor) -> torch.Tensor

      Average softmax predictions over all TTA views.

      :param images: Batch of images with shape ``(B, C, H, W)``.

      :returns: Averaged probability tensor of shape ``(B, num_classes)``.



   .. py:method:: evaluate(test_loader: torch.utils.data.DataLoader, class_names: List[str], model_name: str = 'Model') -> Dict[str, float]

      Evaluate with TTA on a full test set.

      Iterates over ``test_loader``, applies all TTA views to each batch,
      averages the softmax outputs, and computes standard classification
      metrics.

      :param test_loader: DataLoader yielding ``(images, labels)`` tuples.
      :param class_names: Human-readable class names for the report.
      :param model_name: Label used in log messages.

      :returns: Dictionary with ``accuracy``, ``macro_f1``, and ``weighted_f1``.



