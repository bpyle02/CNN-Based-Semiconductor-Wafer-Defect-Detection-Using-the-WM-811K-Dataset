src.training.simclr
===================

.. py:module:: src.training.simclr

.. autoapi-nested-parse::

   SimCLR: A Simple Framework for Contrastive Learning of Visual Representations.

   Implements SimCLR (Chen et al., ICML 2020) for self-supervised pretraining on unlabeled wafer maps.
   Learns useful representations without labels by maximizing agreement between different augmentations.

   .. rubric:: References

   [11] Kang et al. (2021). "WaPIRL: Representation Learning for Wafer Maps". DOI:10.1109/TSM.2021.3064435
   [37] Chen et al. (2020). "SimCLR: Contrastive Learning of Visual Representations". arXiv:2002.05709
   [38] Grill et al. (2020). "BYOL: Bootstrap Your Own Latent". arXiv:2006.07733
   [39] He et al. (2020). "MoCo: Momentum Contrast". arXiv:1911.05722
   [53] (2021). "Semi-Supervised Learning for Wafer Map Defect Classification"
   [78] Schroff et al. (2015). "FaceNet: Triplet Loss". arXiv:1503.03832
   [82] Sohn (2016). "Improved Deep Metric Learning with Multi-Class N-Pair Loss". arXiv:1708.01682
   [85] Caron et al. (2020). "SwAV: Unsupervised Multi-Crop Assignments". arXiv:2006.09882
   [111] Sohn et al. (2020). "FixMatch". arXiv:2001.07685
   [114] Berthelot et al. (2019). "MixMatch". arXiv:1905.02249
   [115] Xie et al. (2020). "UDA: Unsupervised Data Augmentation". arXiv:1904.12848
   [119] Tarvainen & Valpola (2017). "Mean Teacher". arXiv:1703.01780



Attributes
----------

.. autoapisummary::

   src.training.simclr.logger


Classes
-------

.. autoapisummary::

   src.training.simclr.SimCLRProjection
   src.training.simclr.SimCLREncoder
   src.training.simclr.SimCLRLoss
   src.training.simclr.SimCLRPretrainer
   src.training.simclr.ContrastiveBYOLLoss


Module Contents
---------------

.. py:data:: logger

.. py:class:: SimCLRProjection(input_dim: int, hidden_dim: int = 2048, output_dim: int = 128)

   Bases: :py:obj:`torch.nn.Module`


   Projection head for SimCLR.

   Consists of a non-linear projection head: ReLU -> Linear -> normalized output.

   :param input_dim: Dimension of input features from encoder
   :param hidden_dim: Dimension of hidden layer (default 2048)
   :param output_dim: Dimension of output projection (default 128)

   Initialize internal Module state, shared by both nn.Module and ScriptModule.


   .. py:attribute:: proj


   .. py:method:: forward(x: torch.Tensor) -> torch.Tensor

      :param x: Input tensor of shape (B, input_dim)

      :returns: Normalized projection of shape (B, output_dim)



.. py:class:: SimCLREncoder(backbone: torch.nn.Module, feature_dim: int = 512, projection_dim: int = 128)

   Bases: :py:obj:`torch.nn.Module`


   SimCLR encoder: backbone + projection head.

   Wraps existing model (e.g., ResNet, EfficientNet) and adds SimCLR projection head.

   :param backbone: PyTorch model (e.g., ResNet)
   :param feature_dim: Dimension of features from backbone (before projection)
   :param projection_dim: Dimension of projection head output (default 128)

   Initialize internal Module state, shared by both nn.Module and ScriptModule.


   .. py:attribute:: backbone


   .. py:attribute:: projection_head


   .. py:method:: forward(x: torch.Tensor) -> Tuple[torch.Tensor, torch.Tensor]

      :param x: Input tensor of shape (B, C, H, W)

      :returns:     - features: Backbone output of shape (B, feature_dim)
                    - projection: Normalized projection of shape (B, projection_dim)
      :rtype: Tuple of



.. py:class:: SimCLRLoss(temperature: float = 0.07)

   Bases: :py:obj:`torch.nn.Module`


   SimCLR contrastive loss (NT-Xent loss).

   Normalized temperature-scaled cross entropy loss for contrastive learning.

   :param temperature: Temperature parameter for similarity scaling (default 0.07)

   Initialize internal Module state, shared by both nn.Module and ScriptModule.


   .. py:attribute:: temperature
      :value: 0.07



   .. py:method:: forward(z_i: torch.Tensor, z_j: torch.Tensor) -> torch.Tensor

      Compute NT-Xent loss between two augmented views.

      :param z_i: Projections from view i of shape (B, projection_dim)
      :param z_j: Projections from view j of shape (B, projection_dim)

      :returns: Scalar loss value



.. py:class:: SimCLRPretrainer(encoder: SimCLREncoder, optimizer: torch.optim.Optimizer, device: str = 'cuda', temperature: float = 0.07)

   SimCLR pretraining pipeline.

   Handles self-supervised pretraining on unlabeled data.

   :param encoder: SimCLREncoder instance
   :param optimizer: PyTorch optimizer
   :param device: Device to run on (cuda or cpu)
   :param temperature: Temperature for contrastive loss


   .. py:attribute:: encoder


   .. py:attribute:: optimizer


   .. py:attribute:: device
      :value: 'cuda'



   .. py:attribute:: criterion


   .. py:method:: train_epoch(train_loader: torch.utils.data.DataLoader) -> float

      Run one training epoch.

      :param train_loader: DataLoader yielding (images, _) pairs

      :returns: Average loss over epoch



   .. py:method:: pretrain(train_loader: torch.utils.data.DataLoader, epochs: int = 50, scheduler: Optional[torch.optim.lr_scheduler._LRScheduler] = None) -> list

      Run full pretraining loop.

      :param train_loader: DataLoader for unlabeled data
      :param epochs: Number of pretraining epochs
      :param scheduler: Optional learning rate scheduler

      :returns: List of loss values per epoch



   .. py:method:: get_backbone() -> torch.nn.Module

      Extract pretrained backbone for downstream tasks.

      :returns: Backbone model suitable for fine-tuning



.. py:class:: ContrastiveBYOLLoss(temperature: float = 0.1)

   Bases: :py:obj:`torch.nn.Module`


   BYOL-style contrastive loss (non-contrastive alternative to SimCLR).

   BYOL (Bootstrap Your Own Latent) doesn't require negative pairs.
   Uses momentum encoder to create stability.

   :param temperature: Temperature for similarity (default 0.1)

   Initialize internal Module state, shared by both nn.Module and ScriptModule.


   .. py:attribute:: temperature
      :value: 0.1



   .. py:method:: forward(proj_online: torch.Tensor, proj_momentum: torch.Tensor) -> torch.Tensor

      Compute BYOL loss (cosine similarity between normalized projections).

      :param proj_online: Online projection of shape (B, projection_dim)
      :param proj_momentum: Momentum projection of shape (B, projection_dim)

      :returns: Scalar loss value



