src.training.base_trainer
=========================

.. py:module:: src.training.base_trainer

.. autoapi-nested-parse::

   Base trainer class for uniform configuration management.



Attributes
----------

.. autoapisummary::

   src.training.base_trainer.logger


Classes
-------

.. autoapisummary::

   src.training.base_trainer.BaseTrainer


Module Contents
---------------

.. py:data:: logger

.. py:class:: BaseTrainer(config_path: str = 'config.yaml')

   Base trainer that ensures consistent configuration across all training scripts.

   Provides:
   - Config loading from YAML with validation
   - Reproducibility (seed setting across all RNGs)
   - Device management (CPU/GPU)
   - Checkpoint saving/loading with metadata

   Initialize trainer with configuration.

   :param config_path: Path to YAML configuration file


   .. py:attribute:: config


   .. py:attribute:: device


   .. py:attribute:: seed


   .. py:attribute:: checkpoint_dir


   .. py:method:: set_seed() -> None

      Set random seed for reproducibility across all libraries.

      By default leaves ``cudnn.benchmark`` on (~8-15% speedup on Ampere+).
      Set ``self.deterministic = True`` before calling to force exact
      reproducibility at the cost of throughput.



   .. py:method:: save_checkpoint(model: torch.nn.Module, optimizer: torch.optim.Optimizer, epoch: int, metrics: Dict[str, Any], filename: str = 'checkpoint.pth') -> str

      Save training checkpoint with model, optimizer, and metrics.

      :param model: PyTorch model to save
      :param optimizer: Optimizer instance
      :param epoch: Current training epoch
      :param metrics: Dictionary of metrics to save
      :param filename: Checkpoint filename

      :returns: Path to saved checkpoint



   .. py:method:: load_checkpoint(model: torch.nn.Module, optimizer: Optional[torch.optim.Optimizer] = None, checkpoint_path: Optional[Union[str, pathlib.Path]] = None) -> Dict[str, Any]

      Load model and optimizer state from checkpoint.

      :param model: PyTorch model to load state into
      :param optimizer: Optional optimizer to restore state
      :param checkpoint_path: Path to checkpoint. If None, uses latest.

      :returns: Dictionary with epoch and metrics

      :raises TrainingError: If checkpoint not found or cannot be loaded



   .. py:method:: get_training_params() -> Dict[str, Any]

      Get training hyperparameters from config.

      :returns: Dictionary with commonly used training parameters



