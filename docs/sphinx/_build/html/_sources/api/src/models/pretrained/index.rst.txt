src.models.pretrained
=====================

.. py:module:: src.models.pretrained

.. autoapi-nested-parse::

   Pretrained model architectures for transfer learning on wafer defect classification.

   Implements ResNet-18 and EfficientNet-B0 with configurable freezing and
   classifier-head construction utilities.

   .. rubric:: References

   [1] He et al. (2016). "Deep Residual Learning for Image Recognition". arXiv:1512.03385
   [2] Tan & Le (2019). "EfficientNet: Rethinking Model Scaling". arXiv:1905.11946
   [56] (2020). "DL Approaches for Wafer Map Defect Pattern Recognition"
   [60] (2020). "Wafer Defect Pattern Recognition Using Transfer Learning"
   [74] Kornblith et al. (2019). "Do Better ImageNet Models Transfer Better?". arXiv:1805.08974



Attributes
----------

.. autoapisummary::

   src.models.pretrained.logger
   src.models.pretrained.RESNET18_BACKBONE_PREFIXES
   src.models.pretrained.EFFICIENTNET_B0_BACKBONE_PREFIXES
   src.models.pretrained.resnet


Functions
---------

.. autoapisummary::

   src.models.pretrained.build_classifier_head
   src.models.pretrained.set_frozen_prefixes
   src.models.pretrained.resolve_frozen_prefixes
   src.models.pretrained.get_resnet18
   src.models.pretrained.get_efficientnet_b0
   src.models.pretrained.count_params
   src.models.pretrained.get_frozen_params


Module Contents
---------------

.. py:data:: logger

.. py:data:: RESNET18_BACKBONE_PREFIXES
   :type:  Tuple[str, Ellipsis]
   :value: ('conv1', 'bn1', 'layer1', 'layer2', 'layer3', 'layer4')


.. py:data:: EFFICIENTNET_B0_BACKBONE_PREFIXES
   :type:  Tuple[str, Ellipsis]

.. py:function:: build_classifier_head(in_features: int, num_classes: int, dropout_rate: float = 0.5, hidden_dim: Optional[int] = None, hidden_dropout: Optional[float] = None) -> torch.nn.Sequential

   Build a small classifier head with optional hidden layer.

   :param in_features: Input feature dimension from the backbone.
   :param num_classes: Number of output classes.
   :param dropout_rate: Dropout before the head.
   :param hidden_dim: Optional hidden width. When omitted, a single linear layer is used.
   :param hidden_dropout: Dropout after the hidden layer. Defaults to ``dropout_rate``.


.. py:function:: set_frozen_prefixes(model: torch.nn.Module, frozen_prefixes: Sequence[str]) -> None

   Freeze parameters whose names start with any of the provided prefixes.


.. py:function:: resolve_frozen_prefixes(freeze_until: Optional[str], backbone_prefixes: Sequence[str]) -> Tuple[str, Ellipsis]

   Resolve a ``freeze_until`` boundary into a tuple of frozen prefixes.

   ``freeze_until="layer3"`` means freeze everything up to and including
   ``layer3`` and leave later blocks plus the classifier trainable.


.. py:function:: get_resnet18(num_classes: int = 9, pretrained: bool = True, freeze_until: Optional[str] = 'layer3', frozen_prefixes: Optional[Sequence[str]] = None, head_dropout: float = 0.5, head_hidden_dim: Optional[int] = None) -> torch.nn.Module

   ResNet-18 pretrained on ImageNet with configurable fine-tuning.

   :param num_classes: Number of output classes.
   :param pretrained: Load ImageNet weights when available.
   :param freeze_until: Freeze backbone blocks up to this boundary. Set to
                        ``None`` or ``"none"`` to leave the backbone trainable.
   :param frozen_prefixes: Explicit frozen parameter prefixes. When provided,
                           this overrides ``freeze_until``.
   :param head_dropout: Dropout probability applied before the classifier head.
   :param head_hidden_dim: Optional hidden width for a two-layer classifier head.

   :returns: A ResNet-18 model with a configurable classifier head and freeze strategy.


.. py:function:: get_efficientnet_b0(num_classes: int = 9, pretrained: bool = True, freeze_until: Optional[str] = 'features.6', frozen_prefixes: Optional[Sequence[str]] = None, head_dropout: float = 0.5, head_hidden_dim: Optional[int] = None) -> torch.nn.Module

   EfficientNet-B0 pretrained on ImageNet with configurable fine-tuning.

   :param num_classes: Number of output classes.
   :param pretrained: Load ImageNet weights when available.
   :param freeze_until: Freeze backbone blocks up to this boundary. Set to
                        ``None`` or ``"none"`` to leave the backbone trainable.
   :param frozen_prefixes: Explicit frozen parameter prefixes. When provided,
                           this overrides ``freeze_until``.
   :param head_dropout: Dropout probability applied before the classifier head.
   :param head_hidden_dim: Optional hidden width for a two-layer classifier head.

   :returns: An EfficientNet-B0 model with a configurable classifier head and freeze strategy.


.. py:function:: count_params(model: torch.nn.Module) -> Tuple[int, int]

   Count total and trainable parameters.

   :param model: PyTorch model

   :returns: Tuple of (total_parameters, trainable_parameters)


.. py:function:: get_frozen_params(model: torch.nn.Module) -> Tuple[str, Ellipsis]

   Get list of frozen parameter names.

   :param model: PyTorch model

   :returns: Tuple of frozen parameter names


.. py:data:: resnet

