src.models.swin
===============

.. py:module:: src.models.swin

.. autoapi-nested-parse::

   Swin Transformer architecture for wafer defect classification.

   Hierarchical vision transformer using shifted windows for efficient local attention.
   Designed for 96x96 wafer maps with Swin-Tiny scale (~28M params).

   .. rubric:: References

   [101] Liu et al. (2021). "Swin Transformer: Hierarchical Vision Transformer
         using Shifted Windows". arXiv:2103.14030



Classes
-------

.. autoapisummary::

   src.models.swin.DropPath
   src.models.swin.PatchMerging
   src.models.swin.WindowAttention
   src.models.swin.SwinTransformerBlock
   src.models.swin.SwinTransformerStage
   src.models.swin.SwinTransformer


Functions
---------

.. autoapisummary::

   src.models.swin.drop_path
   src.models.swin.window_partition
   src.models.swin.window_reverse
   src.models.swin.get_swin_tiny
   src.models.swin.get_swin_micro


Module Contents
---------------

.. py:function:: drop_path(x: torch.Tensor, drop_prob: float = 0.0, training: bool = False) -> torch.Tensor

   Stochastic depth: randomly drop entire residual branch during training.

   Per-sample binary mask applied to the batch dimension so that each sample
   independently survives or is zeroed out, then rescaled.

   :param x: Input tensor of any shape (B, ...).
   :param drop_prob: Probability of dropping the path.
   :param training: Whether model is in training mode.

   :returns: Tensor of same shape, with paths randomly dropped during training.


.. py:class:: DropPath(drop_prob: float = 0.0)

   Bases: :py:obj:`torch.nn.Module`


   Drop path (stochastic depth) as a module wrapper.

   :param drop_prob: Probability of dropping the path during training.

   Initialize internal Module state, shared by both nn.Module and ScriptModule.


   .. py:attribute:: drop_prob
      :value: 0.0



   .. py:method:: forward(x: torch.Tensor) -> torch.Tensor


.. py:function:: window_partition(x: torch.Tensor, window_size: int) -> torch.Tensor

   Partition feature map into non-overlapping windows.

   :param x: Input tensor of shape (B, H, W, C).
   :param window_size: Size of each square window.

   :returns: Windows tensor of shape (B * num_windows, window_size, window_size, C).


.. py:function:: window_reverse(windows: torch.Tensor, window_size: int, H: int, W: int) -> torch.Tensor

   Reverse window partition back to feature map.

   :param windows: Windows tensor of shape (B * num_windows, window_size, window_size, C).
   :param window_size: Size of each square window.
   :param H: Height of the original feature map.
   :param W: Width of the original feature map.

   :returns: Feature map tensor of shape (B, H, W, C).


.. py:class:: PatchMerging(dim: int)

   Bases: :py:obj:`torch.nn.Module`


   Patch merging layer that reduces spatial resolution by 2x.

   Concatenates 2x2 neighboring patches and projects to 2*dim, halving the
   spatial dimensions while doubling the channel dimension.

   Ref [101]: Section 3.1 -- hierarchical feature maps via patch merging.

   :param dim: Number of input channels.

   Initialize internal Module state, shared by both nn.Module and ScriptModule.


   .. py:attribute:: dim


   .. py:attribute:: reduction


   .. py:attribute:: norm


   .. py:method:: forward(x: torch.Tensor) -> torch.Tensor

      Merge 2x2 patches and project.

      :param x: Input tensor of shape (B, H, W, C).

      :returns: Merged tensor of shape (B, H//2, W//2, 2*C).



.. py:class:: WindowAttention(dim: int, window_size: int, num_heads: int, attention_dropout: float = 0.0, proj_dropout: float = 0.0)

   Bases: :py:obj:`torch.nn.Module`


   Window-based multi-head self-attention with relative position bias.

   Computes attention within local windows of size window_size x window_size.
   A learnable relative position bias table is indexed by the relative
   position between each pair of tokens in the window.

   Ref [101]: Section 3.2 -- shifted window based self-attention.

   :param dim: Number of input channels.
   :param window_size: Size of the attention window.
   :param num_heads: Number of attention heads.
   :param attention_dropout: Dropout rate for attention weights.
   :param proj_dropout: Dropout rate for the output projection.

   Initialize internal Module state, shared by both nn.Module and ScriptModule.


   .. py:attribute:: dim


   .. py:attribute:: window_size


   .. py:attribute:: num_heads


   .. py:attribute:: scale


   .. py:attribute:: relative_position_bias_table


   .. py:attribute:: qkv


   .. py:attribute:: attn_drop


   .. py:attribute:: proj


   .. py:attribute:: proj_drop


   .. py:method:: forward(x: torch.Tensor, mask: Optional[torch.Tensor] = None) -> torch.Tensor

      Compute window attention with relative position bias.

      :param x: Input tensor of shape (num_windows * B, N, C) where N = window_size^2.
      :param mask: Optional attention mask of shape (num_windows, N, N) for shifted
                   window attention. Values of -100.0 block attention; 0.0 allows it.

      :returns: Output tensor of shape (num_windows * B, N, C).



.. py:class:: SwinTransformerBlock(dim: int, num_heads: int, window_size: int = 7, shift_size: int = 0, mlp_ratio: float = 4.0, dropout: float = 0.0, attention_dropout: float = 0.0, drop_path_rate: float = 0.0)

   Bases: :py:obj:`torch.nn.Module`


   Swin Transformer block with window attention and optional shifted window attention.

   Alternates between regular and shifted window partitioning. Even-indexed blocks
   use regular windows; odd-indexed blocks shift by window_size // 2.

   Ref [101]: Section 3.2 -- successive blocks alternate between W-MSA and SW-MSA.

   :param dim: Number of input channels.
   :param num_heads: Number of attention heads.
   :param window_size: Window size for local attention.
   :param shift_size: Shift amount for shifted window attention (0 = regular).
   :param mlp_ratio: Ratio of MLP hidden dim to embedding dim.
   :param dropout: Dropout rate for MLP.
   :param attention_dropout: Dropout rate for attention weights.
   :param drop_path_rate: Stochastic depth rate.

   Initialize internal Module state, shared by both nn.Module and ScriptModule.


   .. py:attribute:: dim


   .. py:attribute:: num_heads


   .. py:attribute:: window_size
      :value: 7



   .. py:attribute:: shift_size
      :value: 0



   .. py:attribute:: mlp_ratio
      :value: 4.0



   .. py:attribute:: norm1


   .. py:attribute:: attn


   .. py:attribute:: drop_path


   .. py:attribute:: norm2


   .. py:attribute:: mlp


   .. py:method:: forward(x: torch.Tensor) -> torch.Tensor

      Apply Swin Transformer block.

      :param x: Input tensor of shape (B, H, W, C).

      :returns: Output tensor of shape (B, H, W, C).



.. py:class:: SwinTransformerStage(dim: int, depth: int, num_heads: int, window_size: int = 7, mlp_ratio: float = 4.0, dropout: float = 0.0, attention_dropout: float = 0.0, drop_path_rates: Optional[list] = None, downsample: bool = True)

   Bases: :py:obj:`torch.nn.Module`


   A stage of Swin Transformer blocks followed by optional patch merging.

   Each stage contains a sequence of Swin blocks that alternate between
   regular and shifted window attention. After the blocks, an optional
   patch merging layer halves the spatial resolution.

   :param dim: Number of input channels.
   :param depth: Number of Swin Transformer blocks in this stage.
   :param num_heads: Number of attention heads.
   :param window_size: Window size for local attention.
   :param mlp_ratio: Ratio of MLP hidden dim to embedding dim.
   :param dropout: Dropout rate.
   :param attention_dropout: Attention dropout rate.
   :param drop_path_rates: Stochastic depth rates for each block in this stage.
   :param downsample: Whether to apply patch merging after this stage.

   Initialize internal Module state, shared by both nn.Module and ScriptModule.


   .. py:attribute:: dim


   .. py:attribute:: depth


   .. py:attribute:: blocks


   .. py:attribute:: downsample


   .. py:method:: forward(x: torch.Tensor) -> torch.Tensor

      Apply stage blocks and optional downsampling.

      :param x: Input tensor of shape (B, H, W, C).

      :returns: (B, H//2, W//2, 2*C).
                Otherwise: (B, H, W, C).
      :rtype: Output tensor. If downsample



.. py:class:: SwinTransformer(image_size: int = 96, patch_size: int = 4, in_channels: int = 3, num_classes: int = 9, embed_dim: int = 96, depths: tuple = (2, 2, 6, 2), num_heads: tuple = (3, 6, 12, 24), window_size: int = 6, mlp_ratio: float = 4.0, dropout: float = 0.0, attention_dropout: float = 0.0, drop_path: float = 0.1)

   Bases: :py:obj:`torch.nn.Module`


   Swin Transformer for image classification.

   Hierarchical architecture with shifted windows for efficient local attention.
   Designed for 96x96 wafer maps.

   Architecture for 96x96 input (Swin-Tiny defaults):
       - Patch embed: 96x96 -> 24x24 patches of dim 96
       - Stage 1: 2 blocks at 24x24, dim=96, heads=3, window=6
       - Merge: 24x24 -> 12x12, dim=192
       - Stage 2: 2 blocks at 12x12, dim=192, heads=6, window=6
       - Merge: 12x12 -> 6x6, dim=384
       - Stage 3: 6 blocks at 6x6, dim=384, heads=12, window=6
       - Merge: 6x6 -> 3x3, dim=768
       - Stage 4: 2 blocks at 3x3, dim=768, heads=24, window=3
       - Global avg pool -> classifier

   Reference: [101] Liu et al. (2021). arXiv:2103.14030

   :param image_size: Input image size (assumed square).
   :param patch_size: Patch size for initial embedding.
   :param in_channels: Number of input channels.
   :param num_classes: Number of output classes.
   :param embed_dim: Base embedding dimension (doubled at each stage).
   :param depths: Number of Swin blocks per stage.
   :param num_heads: Number of attention heads per stage.
   :param window_size: Window size for local attention.
   :param mlp_ratio: Ratio of MLP hidden dim to embedding dim.
   :param dropout: Dropout rate.
   :param attention_dropout: Attention dropout rate.
   :param drop_path: Maximum stochastic depth rate (linearly increases across blocks).

   Initialize internal Module state, shared by both nn.Module and ScriptModule.


   .. py:attribute:: num_classes
      :value: 9



   .. py:attribute:: num_stages


   .. py:attribute:: embed_dim
      :value: 96



   .. py:attribute:: patch_embed


   .. py:attribute:: patch_norm


   .. py:attribute:: pos_drop


   .. py:attribute:: stages


   .. py:attribute:: norm


   .. py:attribute:: head


   .. py:method:: forward(x: torch.Tensor) -> torch.Tensor

      Forward pass.

      :param x: Input tensor of shape (B, C, H, W).

      :returns: Logits of shape (B, num_classes).



.. py:function:: get_swin_tiny(num_classes: int = 9) -> SwinTransformer

   Create a Swin-Tiny model for 96x96 wafer maps.

   Configuration: embed_dim=96, depths=(2,2,6,2), heads=(3,6,12,24), ~28M params.

   :param num_classes: Number of output classes.

   :returns: Initialized SwinTransformer model.


.. py:function:: get_swin_micro(num_classes: int = 9) -> SwinTransformer

   Create a smaller Swin model for faster training.

   Configuration: embed_dim=48, depths=(2,2,2,2), ~3.5M params.

   :param num_classes: Number of output classes.

   :returns: Initialized SwinTransformer model.


